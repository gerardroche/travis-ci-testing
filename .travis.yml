language: php

php:
  - 5.6

install:
  - composer -vvv install

before_script:
  - phpenv config-add test/arbitraryname.ini
  - phpenv config-add test/zarbitraryname.ini
  - php --ini
  - php -i

script:
  - vendor/bin/phpunit -v -c phpunit.xml.dist --coverage-text
