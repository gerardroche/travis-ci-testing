language: php

php:
  - 5.5

env:
  - PHPUNIT_XML=
  - PHPUNIT_XML=_mysql

install:
  - composer -vvv install

script:
  - vendor/bin/phpunit -v -c "phpunit${PHPUNIT_XML}.xml.dist" --coverage-text

cache:
  directories:
    - $HOME/.cache/composer
    - $HOME/.composer/cache
