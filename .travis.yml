language: php

php:
  - 5.6

install:
  - composer -vvv install

before_script:
  - phpenv config-add test/travis-10.ini
  - php --ini
  - php -i

script:
  - vendor/bin/phpunit -v -c phpunit.xml.dist --coverage-text
