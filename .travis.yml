language: php

php:
  - 5.6

install:
  - composer -vvv install

before_script:
  - phpenv config-add test/zx.ini
  - phpenv config-add test/9-x.ini
  - phpenv config-add test/ax.ini
  - phpenv config-add test/9x.ini
  - php --ini
  - php -i

script:
  - vendor/bin/phpunit -v -c phpunit.xml.dist --coverage-text
