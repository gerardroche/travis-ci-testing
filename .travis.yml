language: php

php:
  - 5.5
  - 5.6

install:
  - composer -vvv install

script:
  - vendor/bin/phpunit --verbose

cache:
  directories:
    - $HOME/.cache/composer/files
    - $HOME/.composer/cache/files
