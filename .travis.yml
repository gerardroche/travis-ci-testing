language: php

php:
  - 5.5

env:
  - PHPUNIT_XML=

install:
  - composer -vvv install

script:
  - vendor/bin/phpunit -v -c "phpunit${PHPUNIT_XML}.xml.dist" --coverage-text

cache:
  directories:
    - $HOME/.cache/composer
    - $HOME/.composer/cache
